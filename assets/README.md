# Assets

Drop in:
- `avatar.jpg` (or `.png`) — headshot for the hero, ~400x400 square works best
- `resume.pdf` — your resume, linked by the "Download Resume" button

Filenames must match exactly. If `avatar.jpg` is missing, the hero falls back to a "RS" monogram automatically.
